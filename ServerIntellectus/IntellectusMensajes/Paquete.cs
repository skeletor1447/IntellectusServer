using System;
using System.Collections.Generic;
using System.Text;

namespace IntellectusMensajes
{
    public enum Paquete { LOGIN = 1, LOGINRESPUESTA = 500};
    public enum EstadoLogin { NOLOGUEADO = 0, LOGUEADO = 1};
}
